(function (root, factory) {
  var api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  if (root) root.TeacherApp = api;
})(typeof window !== 'undefined' ? window : globalThis, function () {
  function clone(v) { return JSON.parse(JSON.stringify(v)); }
  function now() { var d = new Date(); return String(d.getHours()).padStart(2, '0') + ':' + String(d.getMinutes()).padStart(2, '0'); }
  var BASE = { loginTabs: [{ key: 'room', label: '房间号登录' }, { key: 'org', label: '学校账户登录' }, { key: 'auto', label: '教务平台唤起' }], courses: [{ id: 'c1', title: '高等数学 · 极限与导数', time: '今天 19:30-21:00', status: '待开播', mode: '真实直播', roomId: 'RM-903271', teacher: '王教授', assistant: '李助教', desc: '课堂练习 + 屏幕板书 + 摄像头答疑' }, { id: 'c2', title: '线性代数专题答疑', time: '今天 14:00-15:30', status: '直播中', mode: '真实直播', roomId: 'RM-903188', teacher: '刘老师', assistant: '陈助教', desc: '矩阵运算重难点精讲，支持实时互动' }, { id: 'c3', title: '英语四级冲刺公开课', time: '明天 20:00-21:00', status: '待开播', mode: '公开课', roomId: 'OPEN-201', teacher: '林老师', desc: '公开课默认全体禁言，适合转发试听' }, { id: 'c4', title: '会计实务录播转直播', time: '昨天 18:30-19:30', status: '已结束', mode: '伪直播', roomId: 'SIM-441', teacher: '周老师', desc: '自动录制回放已生成，可供运营复盘' }], backgrounds: [{ id: 'none', label: '原始画面', style: 'linear-gradient(135deg,#0f172a,#1e293b)' }, { id: 'blur', label: '背景模糊', style: 'linear-gradient(135deg,#1d4ed8,#60a5fa)' }, { id: 'aurora', label: '极光教室', style: 'linear-gradient(135deg,#0f766e,#34d399,#99f6e4)' }, { id: 'studio', label: '演播室', style: 'linear-gradient(135deg,#7c2d12,#fb923c,#fed7aa)' }], students: [{ id: 'stu-1', name: '张同学', muted: false, online: 'PC', attention: 98 }, { id: 'stu-2', name: '李同学', muted: false, online: 'iPad', attention: 92 }, { id: 'stu-3', name: '陈同学', muted: true, online: 'iPhone', attention: 66 }, { id: 'stu-4', name: '刘同学', muted: false, online: 'Mac', attention: 88 }, { id: 'stu-5', name: '何同学', muted: false, online: 'Windows', attention: 81 }], messages: [{ id: 1, user: '系统', role: 'system', time: '19:27', content: '直播间已开启，正在同步教务平台考勤数据。' }, { id: 2, user: '助教小助手', role: 'assistant', time: '19:30', content: '课前 5 分钟完成设备自检，当前网络状态优秀。' }, { id: 3, user: '张同学', role: 'student', time: '19:31', content: '老师好，今天能回顾一下导数定义吗？' }, { id: 4, user: '王教授', role: 'teacher', time: '19:32', content: '可以，先看我共享的课件和板书示例。' }] };
  function resolveLoginPath(kind) { return { room: { route: 'check', contextKey: 'roomDirect' }, org: { route: 'courses', contextKey: 'orgPortal' }, auto: { route: 'live', contextKey: 'autoLaunch' } }[kind] || { route: 'login', contextKey: 'manual' }; }
  function createLiveState() { return { live: { allMuted: false, selectedBackgroundId: 'none', students: clone(BASE.students), messages: clone(BASE.messages) } }; }
  function toggleStudentMute(state, id) { var item = state.live.students.find(function (student) { return student.id === id; }); if (item) item.muted = !item.muted; }
  function toggleAllMute(state) { state.live.allMuted = !state.live.allMuted; state.live.students.forEach(function (student) { student.muted = state.live.allMuted; }); }
  function sendChatMessage(state, content, role) { state.live.messages.push({ id: Date.now(), user: role === 'teacher' ? '王教授' : '系统', role: role, time: now(), content: content }); }
  function applyVirtualBackground(state, id) { state.live.selectedBackgroundId = id; }
  function bootVue() {
    new Vue({ el: '#app', data: function () { return { route: 'login', loginType: 'room', loginContext: 'orgPortal', autoTimer: null, autoLoading: false, liveTimer: null, seconds: 1387, networkSignal: 1, isFullscreen: false, micMeter: 0, speakerMeter: 0, sceneRx: 0, sceneRy: 0, orgStep: 1, selectedCourseId: 'c1', roomDirectCourse: null, forms: { room: '', roomPass: '', org: '', user: '', orgPass: '' }, device: { camStatus: 'pending', micStatus: 'pending', screenStatus: 'idle', camLabel: 'MacBook Pro相机', camOptions: ['MacBook Pro相机'], micLabel: '阵列麦克风', micOptions: ['阵列麦克风', 'USB Podcast Mic', 'AirPods Pro'], speakerLabel: '系统默认扬声器', speakerOptions: ['系统默认扬声器', 'MacBook Pro扬声器'], mirror: true, micTesting: false, speakerTesting: false, vbgId: 'none' }, live: { courseId: 'c1', screenOn: false, camOn: false, micOn: false, allMuted: false, selectedBackgroundId: 'none', pendingMsg: '', chatOpen: true, chatTab: 'chat', studentSearch: '', pipPos: { x: null, y: null }, students: clone(BASE.students), messages: clone(BASE.messages) }, streams: { cam: null, screen: null, mic: null }, ui: { bgModal: false, endConfirm: false, screenMenu: false, checkModal: false, micMenu: false } }; }, computed: { tabs: function () { return BASE.loginTabs; }, courses: function () { return BASE.courses; }, course: function () { if (this.loginContext === 'roomDirect' && this.roomDirectCourse) return this.roomDirectCourse; return BASE.courses.find(function (item) { return item.id === this.selectedCourseId; }, this) || BASE.courses[0]; }, backgrounds: function () { return BASE.backgrounds; }, timerText: function () { var s = this.seconds, h = String(Math.floor(s / 3600)).padStart(2, '0'), m = String(Math.floor(s % 3600 / 60)).padStart(2, '0'), ss = String(s % 60).padStart(2, '0'); return h + ':' + m + ':' + ss; }, bgStyle: function () { return (BASE.backgrounds.find(function (item) { return item.id === this.live.selectedBackgroundId; }, this) || BASE.backgrounds[0]).style; }, bgLabel: function () { return (BASE.backgrounds.find(function (item) { return item.id === this.live.selectedBackgroundId; }, this) || BASE.backgrounds[0]).label; }, onlineCount: function () { return this.live.students.length + 7; }, networkQuality: function () { var s = this.networkSignal; if (s >= 3) return 'good'; if (s >= 2) return 'fair'; return 'poor'; }, liveNotice: function () { if (this.live.allMuted) return '当前全体禁言中，学生无法发言。'; return '课堂秩序正常，聊天已开启敏感词过滤。'; }, checkBgs: function () { return [
          { id: 'none',    label: '不使用', thumb: null },
          { id: 'meeting', label: '背景1', thumb: 'bg-meeting.jpg' },
          { id: 'study',   label: '背景2', thumb: 'bg-study.jpg' },
          { id: 'living',  label: '背景3', thumb: 'bg-living.jpg' }
        ]; } }, created: function () { window.addEventListener('hashchange', this.syncRoute); this.syncRoute(); document.addEventListener('fullscreenchange', () => { this.isFullscreen = !!document.fullscreenElement; }); }, mounted: function () { this.startLiveClock(); var self = this; document.addEventListener('click', function() { self.ui.micMenu = false; }); }, beforeDestroy: function () { window.removeEventListener('hashchange', this.syncRoute); this.stopAllStreams(); clearInterval(this.liveTimer); clearTimeout(this.autoTimer); }, methods: { onVisualMove: function (e) { var r = e.currentTarget.getBoundingClientRect(); var x = (e.clientX - r.left) / r.width - 0.5; var y = (e.clientY - r.top) / r.height - 0.5; this.sceneRy = x * 12; this.sceneRx = y * -8; }, onVisualLeave: function () { this.sceneRx = 0; this.sceneRy = 0; }, orgNextStep: function () { if(!this.forms.user||!this.forms.orgPass) return; this.orgStep=2; this.$nextTick(function(){ if(this.$refs.orgNameInput) this.$refs.orgNameInput.focus(); }); }, triggerAutoLogin: function () { this.autoLoading = true; var self = this; clearTimeout(this.autoTimer); this.autoTimer = setTimeout(function () { self.autoLoading = false; self.selectedCourseId = 'c2'; self.go('live'); }, 2000); }, go: function (route) { location.hash = '#/' + route; }, syncRoute: function () { this.route = (location.hash.replace(/^#\/?/, '').split('?')[0] || 'login'); if (this.route === 'check') this.$nextTick(this.prepareDevices); if (this.route === 'live') { if (this._checkStream) { this._checkStream.getTracks().forEach(function (t) { t.stop(); }); this._checkStream = null; } this.$nextTick(this.activateLiveMedia); } if (this.route === 'login' && this.loginType === 'auto') this.scheduleAutoLogin(); }, startLiveClock: function () { var self = this; clearInterval(this.liveTimer); this.liveTimer = setInterval(function () { self.seconds += 1; if (self.route === 'live') { if (Math.random() < 0.05) { self.networkSignal = Math.floor(Math.random() * 4) + 1; } } }, 1000); }, startMicMeter: function () { var self = this; self.stopMicMeter(); if (!self.streams.mic) return; try { var ctx = new AudioContext(); var src = ctx.createMediaStreamSource(self.streams.mic); var analyser = ctx.createAnalyser(); analyser.fftSize = 256; analyser.smoothingTimeConstant = 0.6; src.connect(analyser); var buf = new Uint8Array(analyser.frequencyBinCount); self._micMeterCtx = ctx; function tick() { if (!self.live.micOn) return; analyser.getByteFrequencyData(buf); var sum = 0; for (var i = 0; i < buf.length; i++) sum += buf[i]; self.micMeter = Math.min(100, Math.round(sum / buf.length / 128 * 200)); self._micMeterRaf = requestAnimationFrame(tick); } tick(); } catch(e) {} }, stopMicMeter: function () { if (this._micMeterRaf) { cancelAnimationFrame(this._micMeterRaf); this._micMeterRaf = null; } if (this._micMeterCtx) { this._micMeterCtx.close(); this._micMeterCtx = null; } this.micMeter = 0; }, scheduleAutoLogin: function () { var self = this; clearTimeout(this.autoTimer); this.autoTimer = setTimeout(function () { self.selectedCourseId = 'c2'; if (window.electronAPI) window.electronAPI.loginSuccess(); self.go('live'); }, 1800); }, submitLogin: function (type) { var target = resolveLoginPath(type); this.loginType = type; this.loginContext = target.contextKey; if (type === 'auto') return this.scheduleAutoLogin(); if (type === 'room') { this.roomDirectCourse = { id: 'room-direct', title: '直播间 ' + (this.forms.room || 'RM-000000'), time: '直接进入', status: '待开播', mode: '真实直播', roomId: this.forms.room, teacher: '当前老师', assistant: '李助教', desc: '通过房间号直接进入直播间' }; } if (type === 'org') this.selectedCourseId = 'c2'; if (window.electronAPI) window.electronAPI.loginSuccess(); this.go(target.route); }, openCourse: function (course) { this.selectedCourseId = course.id; this.go(course.status === '已结束' ? 'courses' : 'check'); }, prepareDevices: async function () { this.device.camStatus = 'checking'; this.device.micStatus = 'checking'; try { if (navigator.mediaDevices && navigator.mediaDevices.getUserMedia) { var stream = await navigator.mediaDevices.getUserMedia({ video: true, audio: true }); this.$nextTick(function () { if (this.$refs.camVideo) { this.$refs.camVideo.srcObject = stream; this.$refs.camVideo.onloadedmetadata = () => this.startBgCanvas(); } }.bind(this)); this.device.camStatus = 'ok'; this.device.micStatus = 'ok'; this._checkStream = stream; } else throw new Error('unsupported'); } catch (e) { this.device.camStatus = 'mock'; this.device.micStatus = 'mock'; } }, selectCheckBg: function (id) { this.device.vbgId = id; }, startBgCanvas: function () {
      var self = this;
      var video = self.$refs.camVideo;
      var canvas = self.$refs.bgCanvas;
      if (!canvas || !video) return;
      canvas.width = video.videoWidth || 640;
      canvas.height = video.videoHeight || 480;
      var ctx = canvas.getContext('2d');

      // 预加载背景图
      var bgImages = {};
      var bgUrls = {
        meeting: 'bg-meeting.jpg',
        study:   'bg-study.jpg',
        living:  'bg-living.jpg'
      };
      Object.keys(bgUrls).forEach(function(key) {
        var img = new Image();
        if (bgUrls[key].startsWith('http')) img.crossOrigin = 'anonymous';
        img.src = bgUrls[key];
        bgImages[key] = img;
      });

      // 初始化 MediaPipe SelfieSegmentation
      var segmentation = new SelfieSegmentation({
        locateFile: function(file) {
          return 'https://cdn.jsdelivr.net/npm/@mediapipe/selfie_segmentation@0.1.1675465747/' + file;
        }
      });
      segmentation.setOptions({ modelSelection: 1, selfieMode: false });

      segmentation.onResults(function(results) {
        var id = self.device.vbgId;
        var w = canvas.width, h = canvas.height;
        ctx.clearRect(0, 0, w, h);

        // 1. 先画背景
        if (id === 'white') {
          ctx.fillStyle = '#f0f0f0';
          ctx.fillRect(0, 0, w, h);
        } else if (bgImages[id] && bgImages[id].complete && bgImages[id].naturalWidth) {
          ctx.drawImage(bgImages[id], 0, 0, w, h);
        }

        // 2. 离屏 canvas 处理人像（蒙版和画面都镜像）
        var offscreen = document.createElement('canvas');
        offscreen.width = w; offscreen.height = h;
        var octx = offscreen.getContext('2d');

        // 画蒙版（镜像），加模糊让边缘羽化
        octx.filter = 'blur(6px)';
        octx.save();
        if (self.device.mirror) { octx.translate(w, 0); octx.scale(-1, 1); }
        octx.drawImage(results.segmentationMask, 0, 0, w, h);
        octx.restore();
        octx.filter = 'none';

        // source-in：只保留蒙版区域内的人像
        octx.globalCompositeOperation = 'source-in';
        octx.save();
        if (self.device.mirror) { octx.translate(w, 0); octx.scale(-1, 1); }
        octx.drawImage(results.image, 0, 0, w, h);
        octx.restore();

        // 3. 把人像贴到主 canvas 背景上面
        ctx.drawImage(offscreen, 0, 0);
      });

      // 推帧循环
      self._segmentation = segmentation;
      function loop() {
        if (!self.$refs.bgCanvas || self.device.vbgId === 'none') {
          self._segRaf = requestAnimationFrame(loop);
          return;
        }
        segmentation.send({ image: video }).then(function() {
          self._segRaf = requestAnimationFrame(loop);
        });
      }
      loop();
    }, startPipBgCanvas: function () { var self = this; var video = self.$refs.pipVideo; var canvas = self.$refs.pipBgCanvas; if (!canvas || !video) return; canvas.width = video.videoWidth || 640; canvas.height = video.videoHeight || 480; var ctx = canvas.getContext('2d'); var bgImages = {}; var bgUrls = { meeting: 'bg-meeting.jpg', study: 'bg-study.jpg', living: 'bg-living.jpg' }; Object.keys(bgUrls).forEach(function(key) { var img = new Image(); img.src = bgUrls[key]; bgImages[key] = img; }); var segmentation = new SelfieSegmentation({ locateFile: function(file) { return 'https://cdn.jsdelivr.net/npm/@mediapipe/selfie_segmentation@0.1.1675465747/' + file; } }); segmentation.setOptions({ modelSelection: 1, selfieMode: false }); segmentation.onResults(function(results) { var id = self.device.vbgId; var w = canvas.width, h = canvas.height; ctx.clearRect(0, 0, w, h); if (bgImages[id] && bgImages[id].complete && bgImages[id].naturalWidth) { ctx.drawImage(bgImages[id], 0, 0, w, h); } var offscreen = document.createElement('canvas'); offscreen.width = w; offscreen.height = h; var octx = offscreen.getContext('2d'); octx.filter = 'blur(6px)'; octx.save(); if (self.device.mirror) { octx.translate(w, 0); octx.scale(-1, 1); } octx.drawImage(results.segmentationMask, 0, 0, w, h); octx.restore(); octx.filter = 'none'; octx.globalCompositeOperation = 'source-in'; octx.save(); if (self.device.mirror) { octx.translate(w, 0); octx.scale(-1, 1); } octx.drawImage(results.image, 0, 0, w, h); octx.restore(); ctx.drawImage(offscreen, 0, 0); }); self._segmentation = segmentation; function loop() { if (!self.$refs.pipBgCanvas || !self.device.vbgId || self.device.vbgId === 'none') { self._segRaf = requestAnimationFrame(loop); return; } segmentation.send({ image: video }).then(function() { self._segRaf = requestAnimationFrame(loop); }); } loop(); }, testSpeaker: function () { var a = new AudioContext(); var o = a.createOscillator(); o.connect(a.destination); o.frequency.value = 440; o.start(); setTimeout(function(){ o.stop(); a.close(); }, 600); }, toggleMicTest: function () { var self = this; if (self.device.micTesting) { self.device.micTesting = false; if (self._micAnalyserRaf) { cancelAnimationFrame(self._micAnalyserRaf); self._micAnalyserRaf = null; } if (self._micTestStream) { self._micTestStream.getTracks().forEach(function(t){ t.stop(); }); self._micTestStream = null; } self.micMeter = 0; return; } self.device.micTesting = true; navigator.mediaDevices.getUserMedia({ audio: true }).then(function(stream) { self._micTestStream = stream; var ctx = new AudioContext(); var src = ctx.createMediaStreamSource(stream); var analyser = ctx.createAnalyser(); analyser.fftSize = 256; src.connect(analyser); var buf = new Uint8Array(analyser.frequencyBinCount); function tick() { if (!self.device.micTesting) return; analyser.getByteFrequencyData(buf); var sum = 0; for (var i = 0; i < buf.length; i++) sum += buf[i]; self.micMeter = Math.min(100, Math.round(sum / buf.length / 128 * 100 * 2.5)); self._micAnalyserRaf = requestAnimationFrame(tick); } tick(); }).catch(function() { self.device.micTesting = false; }); }, toggleSpeakerTest: function () { var self = this; if (self.device.speakerTesting) { self.device.speakerTesting = false; if (self._speakerCtx) { self._speakerCtx.close(); self._speakerCtx = null; } if (self._speakerMeterInterval) { clearInterval(self._speakerMeterInterval); self._speakerMeterInterval = null; } self.speakerMeter = 0; return; } self.device.speakerTesting = true; var ctx = new AudioContext(); self._speakerCtx = ctx; var notes = [261.63, 329.63, 392.00, 523.25]; var gain = ctx.createGain(); gain.gain.setValueAtTime(0.18, ctx.currentTime); gain.connect(ctx.destination); notes.forEach(function(freq, i) { var o = ctx.createOscillator(); o.type = 'sine'; o.frequency.value = freq; o.connect(gain); o.start(ctx.currentTime + i * 0.18); o.stop(ctx.currentTime + i * 0.18 + 0.55); }); var totalDur = notes.length * 0.18 + 0.55; var elapsed = 0; self._speakerMeterInterval = setInterval(function() { elapsed += 0.1; var progress = elapsed / totalDur; var level = Math.sin(progress * Math.PI); self.speakerMeter = Math.round(level * 85 + 10); if (elapsed >= totalDur) { clearInterval(self._speakerMeterInterval); self._speakerMeterInterval = null; self.speakerMeter = 0; self.device.speakerTesting = false; if (self._speakerCtx) { self._speakerCtx.close(); self._speakerCtx = null; } } }, 100); }, testScreen: async function () { try { if (navigator.mediaDevices && navigator.mediaDevices.getDisplayMedia) { this.streams.screen = await navigator.mediaDevices.getDisplayMedia({ video: true }); this.device.screenStatus = 'ok'; this.live.screenOn = true; } else throw new Error('unsupported'); } catch (e) { this.device.screenStatus = 'mock'; this.live.screenOn = true; } }, activateLiveMedia: function () { if (this.streams.screen && this.$refs.screenVideo) { this.live.screenOn = true; this.$refs.screenVideo.srcObject = this.streams.screen; } }, toggleScreen: async function () { if (this.live.screenOn) { if (this.streams.screen) this.streams.screen.getTracks().forEach(function (track) { track.stop(); }); this.streams.screen = null; this.live.screenOn = false; return; } await this.testScreen(); this.activateLiveMedia(); }, toggleCam: async function () { if (this.live.camOn) { if (this.streams.cam) { this.streams.cam.getTracks().forEach(function (t) { t.stop(); }); this.streams.cam = null; } if (this.$refs.pipVideo) this.$refs.pipVideo.srcObject = null; if (this._segRaf) { cancelAnimationFrame(this._segRaf); this._segRaf = null; } this.live.camOn = false; } else { try { this.streams.cam = await navigator.mediaDevices.getUserMedia({ video: true }); this.live.camOn = true; this.$nextTick(function () { var video = this.$refs.pipVideo; if (video) { video.srcObject = this.streams.cam; if (this.device.vbgId && this.device.vbgId !== 'none') { video.onloadedmetadata = () => this.startPipBgCanvas(); } } }.bind(this)); } catch (e) { this.live.camOn = false; } } }, toggleMic: function () { var self = this; if (self.live.micOn) { self.live.micOn = false; self.stopMicMeter(); if (self.streams.mic) { self.streams.mic.getTracks().forEach(function (t) { t.stop(); }); self.streams.mic = null; } } else { navigator.mediaDevices.getUserMedia({ audio: true }).then(function (stream) { self.streams.mic = stream; self.live.micOn = true; self.$nextTick(self.startMicMeter); }).catch(function () {}); } }, toggleStudentMute: function (student) { toggleStudentMute({ live: this.live }, student.id); }, toggleAllMute: function () { toggleAllMute({ live: this.live }); }, pushQuickMessage: function () { if (!this.live.pendingMsg.trim()) return; sendChatMessage({ live: this.live }, this.live.pendingMsg, 'teacher'); this.live.pendingMsg = ''; this.$nextTick(this.scrollChat); }, sendChat: function () { this.pushQuickMessage(); }, toggleChat: function () { this.live.chatOpen = !this.live.chatOpen; }, toggleFullscreen: function () { var el = document.documentElement; if (!document.fullscreenElement) { el.requestFullscreen && el.requestFullscreen(); } else { document.exitFullscreen && document.exitFullscreen(); } }, pipDragStart: function (e) { var self = this; var pip = e.currentTarget; var rect = pip.getBoundingClientRect(); var ox = e.clientX - rect.left; var oy = e.clientY - rect.top; function onMove(ev) { var parent = pip.parentElement.getBoundingClientRect(); var x = ev.clientX - parent.left - ox; var y = ev.clientY - parent.top - oy; x = Math.max(0, Math.min(parent.width - rect.width, x)); y = Math.max(0, Math.min(parent.height - rect.height, y)); self.live.pipPos = { x: x, y: y }; } function onUp() { document.removeEventListener('mousemove', onMove); document.removeEventListener('mouseup', onUp); } document.addEventListener('mousemove', onMove); document.addEventListener('mouseup', onUp); e.preventDefault(); }, selectBackground: function (id) { applyVirtualBackground({ live: this.live }, id); this.ui.bgModal = false; }, scrollChat: function () { var el = this.$refs.chatList; if (el) el.scrollTop = el.scrollHeight; }, confirmEnd: function () { this.ui.endConfirm = false; this.stopAllStreams(); this.live.screenOn = false; this.go(this.loginContext === 'roomDirect' ? 'login' : 'courses'); }, stopAllStreams: function () { this.stopMicMeter(); if (this._segRaf) { cancelAnimationFrame(this._segRaf); this._segRaf = null; } if (this._segmentation) { this._segmentation.close(); this._segmentation = null; } ['cam', 'screen', 'mic'].forEach(function (key) { var stream = this.streams[key]; if (stream && stream.getTracks) stream.getTracks().forEach(function (track) { track.stop(); }); this.streams[key] = null; }, this); } }, watch: { loginType: function (value) { if (this.route === 'login' && value === 'auto') { this.autoLoading = false; } this.orgStep = 1; }, route: function () { this.$nextTick(this.scrollChat); } } });
  }
  if (typeof window !== 'undefined' && window.Vue) window.addEventListener('DOMContentLoaded', bootVue);
  return { resolveLoginPath: resolveLoginPath, createLiveState: createLiveState, toggleStudentMute: toggleStudentMute, toggleAllMute: toggleAllMute, sendChatMessage: sendChatMessage, applyVirtualBackground: applyVirtualBackground };
});
